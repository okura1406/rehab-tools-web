# CLAUDE.md — このリポジトリで作業するときの決まり

リハビリテーション部門の評価尺度ツールを、スマートフォン向けに公開しているリポジトリ。
公開URL: <https://okura1406.github.io/rehab-tools-web/>

**このリポジトリは Public（公開）です。** URLを知っていれば誰でも閲覧できます。

---

## まず読むもの

新しいツールを作る・直すときは、**必ず [contrib/ツール作成の手引き.md](contrib/ツール作成の手引き.md) を先に読む。**
採点の出し方、QRコードの作り方、下書き保存の仕様、ボタンの呼び方、出す前のチェックリストまで、
現行の決まりがすべて書いてある。既存ツールはこの手引きに沿って作られている。

---

## いちばん大事なこと：直接編集してよいファイルは限られている

| ファイル | 扱い |
|---|---|
| `battery.html` `pain.html` `updrs.html` `amsd.html` `token.html` `rcpm.html` `lexdec*.html` `noun.html` `verb.html` `ncomp.html` `vcomp.html` `syn.html` | **編集禁止。** 管理者のパソコンにある正本から自動生成される |
| `index.html` `st.html` | このリポジトリが正本。直接編集してよい（ツール一覧のページ） |
| `sw.js` | オフライン保存の設定。ツールを増やしたら `PRECACHE_URLS` に追記する |
| `contrib/` | **新しいツール・修正案はここに置く**（下記） |
| `.github/workflows/` | contrib への新着を Slack へ知らせる仕組み |
| `Cloudflare移行手順.md` | 公開先を Cloudflare Pages へ移し、許可した人だけが開ける状態にする手順 |

**公開用HTMLを直接書き換えると、次に管理者がパソコンから公開したときに、その変更は消える。**
正本（`評価バッテリー　QRコード/` フォルダ）は管理者のパソコンの中にあり、このリポジトリには入っていない。

---

## 新しいツールを作るとき（スマホからの作業もこれ）

1. [contrib/ツール作成の手引き.md](contrib/ツール作成の手引き.md) を読む
2. 既存のツールを参考にする（`contrib/類義語判断検査.html` が現行仕様に沿った最新の例）
3. **`contrib/` に日本語のファイル名で置く**（例：`contrib/嚥下評価ツール.html`）
4. コミットする。公開用の名前への変換・一覧への追加・オフライン対応の組み込みは、
   管理者がパソコン側の「新着をとりこむ.bat」で行う

**`contrib/` の外（リポジトリのトップ）には置かない。** 置くと、オフライン対応もツール一覧への掲載も入らないまま公開されてしまう。

### 作るときに必ず守ること（手引きの要点）

- **1つのHTMLファイルで完結させる。** 外部のライブラリ・CSS・フォント・画像を読み込まない
  （電波の届かない場所で動かなくなるため。QRコード生成も既存ツールから埋め込みコードを流用する）
- 入力は `localStorage` に保存し、**外部へ送信しない**（`fetch` などを書かない）
- 保存キー（`const NS`）は他のツールと重複しない名前にする
- 下書きの保存枠は3つ（`const SLOTS=['A','B','C']`）
- 評価日欄・前回との比較は作らない（廃止済みの仕様）
- スマートフォンの画面幅（375px）で横スクロールが出ないようにする
- 氏名はQRコードに入れない

---

## 置いてはいけないもの

**患者の氏名・ID・生年月日、職員の氏名を含むファイルは、テスト用であっても絶対に置かない。**
このリポジトリは公開されており、一度コミットすると履歴に残って完全には消せない。

勤務表・VF画像・カルテの記載も同様に置かない。

---

## 公開までの流れ

```
contrib/ にファイルを置く
      ↓ （自動）Slack に新着通知
管理者がパソコンで「新着をとりこむ.bat」を実行
      ↓ 中身を目で確認
正本へ取り込み → 一覧・オフライン設定へ登録 → 公開
```

**このリポジトリへのコミットだけでは、ツールは公開サイトの一覧に出ない。**
管理者のパソコンでの取り込み作業が必要。急ぐ場合はその旨を管理者に伝えること。

`sw.js` の `CACHE_VERSION` は公開時に自動で上がる。手で変更しない。

---

## 仕様書について

各ツールの詳細仕様書（`評価バッテリー詳細仕様書_*.md`）は、管理者のパソコン側にある。
このリポジトリからは見えないので、仕様の意図が分からないときは推測で変更せず、管理者に確認する。
