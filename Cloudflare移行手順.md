# Cloudflare Pages + Access への移行手順

公開先を GitHub Pages から Cloudflare Pages へ移し、**許可した人だけが開ける状態**にするための手順書。

> **2026-09-14 更新。** 実際に手順A〜Eまで実施し、画面の名称・操作が当初の想定と大きく違っていた箇所と、
> 途中で判明した不具合をすべて反映済み。以下は「実際にこうだった」という記録でもある。

## なぜ移すのか

いまのサイトは `robots.txt` と `noindex` で検索避けをしているが、これは**検索避けであってアクセス制限ではない**。
URLを知っていれば誰でも開けるし、GitHubリポジトリが公開なのでコード検索・クローンでも中身が読める。

評価尺度の項目文・採点基準を全文掲載しているため、不特定多数が閲覧できる状態は避けたい。

| | いま（GitHub Pages） | 移行後（Cloudflare Pages + Access） |
|---|---|---|
| 部外者の閲覧 | URLを知っていれば誰でも | 許可リストの人だけ |
| 退職者を締め出す | できない | 即座にできる |
| オフライン動作 | できる | できる（検証済み） |
| 新規インストール | できる | できる（検証済み） |
| 費用 | 0円 | **0円**（50人まで。独自ドメインも不要だった） |

---

## 実施状況（2026-09-14 時点）

| 手順 | 状態 |
|---|---|
| A：Cloudflare アカウント作成 | 完了 |
| B：Pages にリポジトリを接続 | 完了 → `rehab-tools-web.pages.dev` |
| C：Zero Trust 有効化 | 完了（Free、チーム名 `rehab-tools`） |
| D：Access で鍵をかける | 完了 |
| E：検証 | **全項目クリア** |
| F：本番切り替え（スタッフへ案内） | **未実施** |
| G：GitHub リポジトリを private に | **未実施** |

**現在、許可リストには管理者のアドレス1件のみ。** スタッフの追加は手順Fで行う。

---

## 大前提：GitHub Pages は止めずに並行構築する

**いまのサイトを動かしたまま、別URLで新しい方を作って試す。**
検証が終わってから切り替える。だから途中で失敗してもスタッフへの影響はゼロ。

```
今のサイト（動いたまま）  ────────────────────────→ 検証OK後に案内を差し替え
                                                        ↓
        新しいサイトを並行して構築 → 自分の端末で検証 ─┘ → GitHubをprivate化
```

**順番を守ること。** 先にGitHubをprivateにすると、無料プランでは GitHub Pages が止まり、
サイトが404になってスタッフ全員が使えなくなる。必ず新しい配信先を用意してから。

---

## 事前準備

- Cloudflare に登録するメールアドレス（管理者のもの）
- 許可するスタッフのメールアドレス一覧（個人アドレスで可。院内メール・Gmail混在OK）
- 検証用のスマートフォン1台（できれば2台）
- テスト用に、**自分が持っている別のメールアドレス**1件（「弾かれるか」の確認に使う）

> **このリポジトリに実際のメールアドレスを書かないこと。** 許可リストは Cloudflare の管理画面にだけ入れる。

---

## 手順A：Cloudflare アカウントを作る

1. <https://dash.cloudflare.com/sign-up> でアカウント作成
2. 届いたメールから認証を済ませる

クレジットカードの登録は不要。

> 登録直後に「ドメインを追加しますか」と誘導されるが、**何も追加せずスキップする。**
> 独自ドメインは不要（手順D-2で確認済み）。

---

## 手順B：Pages にリポジトリをつなぐ

### ⚠ ここが最大の罠

Cloudflare は新規作成の導線を **Workers** に寄せており、**Pages は「legacy」扱いで隠れている。**
案内どおりに進むと Workers の作成画面に入ってしまい、このリポジトリでは動かない。

### B-1. Pages の作成画面にたどり着く

1. 左メニュー **Compute** → **Workers & Pages**
   （旧デザインの「Workers & Pages」は Compute の中に移動している）
2. **Create application**
3. 「Make something new」という画面が出る
4. **上に並んでいる選択肢は使わない。** 一番下の小さいリンク
   **「Need to use the legacy Pages workflow? → Continue to Pages」** をクリック
5. 「Get started with Pages」画面で **Import an existing Git repository** → **Get started**

> **「Continue with GitHub」を押してはいけない。** Workers の作成画面（Deploy command に
> `npx wrangler deploy` が入っているもの）に入る。このリポジトリには wrangler の設定ファイルが
> ないので必ず失敗し、さらに `rehab-tools-web` というプロジェクト名を Workers 側に取られる。
> 間違えたら **Deploy を押さずに Back** で戻ること。

### B-2. リポジトリを選ぶ

GitHub との連携画面で、**「Only select repositories」を選び `rehab-tools-web` だけ**にチェックを入れる。
「All repositories」でも動くが、必要のない権限を渡さずに済む。

### B-3. ビルド設定

| 項目 | 値 |
|---|---|
| Project name | `rehab-tools-web`（これがそのままURLになる。後から変更不可と考えること） |
| Production branch | `main` |
| Framework preset | **None** |
| Build command | **空欄** |
| Build output directory | `/`（薄い文字の `/` は入力例なので空欄のままでよい） |
| Root directory | **空欄** |

> このサイトはHTMLを置くだけの静的サイトで、ビルド処理は一切ない（`package.json` もない）。
> Build command に何か入っていると必ず失敗する。
> **すべて相対パス**で書かれており、`manifest.webmanifest` の `start_url` と `scope` も `"./"` なので、
> URLが変わってもHTML・PWA設定の書き換えは不要。

4. **Save and Deploy** → 1分ほどで `rehab-tools-web.pages.dev` が発行される

この時点では**まだ誰でも見られる**。次の手順で鍵をかける。

### B-4. 以後の更新について

**main への push で、GitHub Pages と Cloudflare Pages の両方が自動更新される。**
管理者PCのこれまでの公開手順（正本 → push）を変える必要はない。
併存期間中も両方の内容がずれない。

---

## 手順B補足：`sw.js` の転送問題（対処済み・記録）

**症状：** トップページは開けるのに、各ツール（`battery.html` など）が `ERR_FAILED` で開けない。

**原因：** Cloudflare Pages には `/battery.html` への要求を `/battery` へ**自動転送する**仕様がある
（GitHub Pages にはない）。転送を経て取得した応答には「転送された」印がつき、
ブラウザは**その保存データをページ表示に使うことを安全上の理由で拒否する**。
`sw.js` は17個のツールを一括保存するため、全ツールがこの状態になっていた。
トップページ（`/`）だけは転送の対象外なので無事だった。

**対処：** `sw.js` に、保存前に転送印を外す処理を追加済み（2026-09-14、PR #2）。
転送が起きない GitHub Pages では素通りするだけなので、**両方の配信先で同じファイルが使える。**

> **PC側の正本に `sw.js` があるなら、修正版を反映しておくこと。**
> 次回の公開で上書きされると、この問題が再発する。

**検証するときの注意：** 端末に古い保存データが残っていると、直っていても同じエラーが再現する。
**InPrivate ウィンドウで確認する**か、サイトデータを削除してから試すこと。

---

## 手順C：Zero Trust を有効にする

1. 左メニュー **Zero Trust** → **Get started**
2. プラン選択画面で **Zero Trust Free**（$0 / seat / month、50シート）の **Select plan**
3. 支払い情報の入力を求められることがあるが、**Free のままなら請求は発生しない**
   （Settings で `Seats: 0 of 50` / `Price: $0 / month` と確認できる）

### チーム名は自動生成される

**チーム名を自分で決める画面は出ない。** `spring-queen-73a5` のような名前が勝手に付く。
これは**スタッフが見る認証画面のURL**（`〇〇.cloudflareaccess.com`）になるので、変更しておくこと。

**Settings**（左メニュー最下部）→ ページ最上部の **Team name** → **Edit** → `rehab-tools` に変更。
すぐ下の **Team domain** が `rehab-tools.cloudflareaccess.com` になっていれば成功。

> 変更後も、**開いたままのログイン画面には古い名前が残る。** 画面を開き直して確認すること。
> 「変わっていない」と誤解しやすい。

---

## 手順D：Access で鍵をかける

### D-1. ログイン方法を確認する

**One-time PIN は既定で登録済み**なので、追加作業は不要。

確認するなら **Ctrl + K** → `identity` → **Integrations > Identity providers**。
`One-time PIN` と `Cloudflare` の2つに `ADDED` と表示されているはず。

> **`Cloudflare` が曲者。** これは Cloudflare アカウントの持ち主しか使えないのに、
> ログイン画面には選択肢として出てしまう。スタッフにとっては「押しても入れないボタン」になる。
> **D-4 で消す。**

### D-2. アプリケーションを作る

**Access controls** → **Applications** → **Add an application** → **Self-hosted and private**
→ **Continue with Self-hosted and private**

> 途中に `Private destinations / Workers / Public DNS / Service auth` の帯が出るが、
> これは説明図の切り替えにすぎない。どれを選んでも次の画面は同じ。

#### Destinations（ここが2つ目の罠）

1. **+ Add public hostname** をクリック
2. 出てきた行の Subdomain 欄の下にある **「Switch to custom input」** をクリック
3. 現れた欄に `rehab-tools-web.pages.dev` と入力（`https://` は付けない。Path は空欄）
4. **Workers の空行が残っていたらゴミ箱アイコンで削除する**（残すと保存でエラーになる）

> 「Select Domain」のドロップダウンは、**自分がCloudflareに登録したドメインしか出てこない。**
> custom input に切り替えないと `.pages.dev` は指定できない。
> ここさえ抜ければ**独自ドメインの購入は不要**（2026-09-14 に実際に通ることを確認済み）。

#### Details

| 項目 | 値 |
|---|---|
| Name | `リハ評価ツール`（**スタッフの認証画面に表示される**ので分かりやすい名前にする） |
| Session Duration | **1 month**（既定は 24 hours。変更しないと毎日認証させることになる） |

### D-3. 誰を通すかを決める

**Access policies** → **Create new policy**

| 項目 | 値 |
|---|---|
| Policy Name | `リハ部スタッフ` |
| Action | **Allow** |
| Include → Selector | **Emails** |
| Include → Value | 許可するアドレスを1件ずつ入力 |

- **`Emails ending in` と間違えないこと。** そちらはドメイン単位（`@gmail.com` の人全員）の指定になる
- **Policy session duration** は `Same as application session duration` のままでよい
- **MFA / Just-in-time access** は Off のまま

> **検証が終わるまでは、自分のアドレス1件だけにしておく。**
> 動かない設定を全員に配ってしまう事故を防げる。

**院内Wi-Fiでは認証を省きたい場合**は、もう1つポリシーを追加する（任意）。

| 項目 | 値 |
|---|---|
| Policy name | 院内ネットワーク |
| Action | **Bypass** |
| Include | **IP ranges** → 院内のグローバルIP |

院内では何も聞かれず、訪問リハなど院外のときだけ認証が入る。
グローバルIPは情報システム担当に確認すること（固定IPでないと使えない）。

> **このBypassポリシーは、手順Eの検証が全部終わってから作ること。**
> 先に作ると院内から素通りになり、「許可外のアドレスが弾かれるか」のテストができなくなる。

### D-4. ログイン方法を One-time PIN だけに絞る

アプリの **Authentication** セクションで：

1. **「Accept all available identity providers」を OFF**
2. **「Choose available identity providers for this application」で One-time PIN だけを選ぶ**
3. **「Apply instant authentication」を ON**

3番はログイン方法が1つのときだけ有効にできる。ONにすると**ログイン方法を選ぶ画面が丸ごと消え、
いきなりメールアドレス入力欄が出る。** スタッフの操作が1画面分減り、
「押しても入れない Cloudflare ボタン」も消える。

> この変更後は**管理者も6桁の数字で入ることになる。**
> スタッフと同じ画面で検証できるようになるので、むしろ好都合。

---

## 手順E：検証チェックリスト

**ここを省かないこと。** 全員に案内する前に、自分の端末で全部確認する。
以下は 2026-09-14 に全項目クリア済み。スタッフ追加後や設定変更後は再確認すること。

### 認証まわり

- [x] 許可したアドレスでログインでき、ツールが開ける
- [x] **許可していないアドレスでは弾かれる**（自分の別アドレスで確認）
- [x] 6桁の数字がメールで届く（迷惑メールフォルダも確認）

> **許可外のアドレスには、そもそも6桁の数字が届かない。**
> 「コードを送りました」という画面は出るが、実際には送られない（アドレスの存在を漏らさないための仕組み）。
> 「メールが来ない＝弾かれている」であって、故障ではない。
> 判断に迷ったら、**許可済みのアドレスで同じ操作をして、そちらには届くこと**を確認すればよい。

### PWA（いちばん引っかかりやすい）

- [x] Safari で開く → **ホーム画面に追加** → **アイコンから起動できる**
- [x] ホーム画面アプリ側で再認証を求められた場合、その画面で認証を完了できる
- [x] 認証後、アプリとして正常に表示される（アドレスバーが出ない standalone 表示）

> ホーム画面アプリは Safari と別扱いになることがあり、もう一度認証が必要な場合がある。
> **手順を「先にホーム画面へ追加 → アプリから開いて認証」に固定する**と混乱が少ない。

### 動作確認

- [x] 機内モードにして**オフラインで起動できる**（`sw.js` のキャッシュが効いているか）
- [x] QRコードが生成される
- [x] 下書き保存 A / B / C が動く
- [x] 画面幅375pxで横スクロールが出ない
- [x] 17個のツールすべてが開ける
      （index / st / battery / pain / updrs / amsd / token / rcpm /
       lexdec 1〜4 / noun / verb / ncomp / vcomp / syn）

### 複数端末

- [ ] **2台目の端末で同じメールアドレスを使ってログインできる**
      （Access は「人」を認証するので端末数の制限はない。1人が何台入れても1ユーザー扱い）

---

## 手順F：本番切り替え

1. **スタッフのアドレスをポリシーに追加する**
   Access controls → Applications → リハ評価ツール → ポリシー `リハ部スタッフ` → Include の Emails に追加
2. スタッフへ新URLを案内（下の文例を参照）
3. 各自にホーム画面へ追加してもらう
4. 全員が移行できたことを確認する

> **移行前に必ずアナウンスすること：保存中の下書きは使い切ってもらう。**
> 下書きは `localStorage` に入っており、**ドメインが変わると引き継がれない**。
> 評価途中のデータが消えて困る人がいないタイミング（週明けなど）を選ぶ。

---

## 手順G：GitHub リポジトリを private にする

**手順Fまで完了し、全員が新URLに移ってから実施すること。**

GitHub → リポジトリ → **Settings** → **General** → 最下部 **Danger Zone**
→ **Change repository visibility** → **Make private**

これでコード検索・クローン・新規アーカイブ収集が止まる。
Cloudflare Pages は private リポジトリからでも配信を続けるので、サイトに影響はない。

GitHub Pages は無料プランでは停止するが、配信は Cloudflare が担っているので問題ない。

private化のあと、`README.md` と `CLAUDE.md` の記述（公開URL・「このリポジトリは Public です」の記載）を更新すること。

---

## スタッフ案内文（そのまま使える文例）

新URLのQRコードをリポジトリのルートに置いてある。印刷し直したいときはこれを使う。

| ファイル | 用途 |
|---|---|
| `リハ評価ツール_QRコード.png` | メール添付・Slack・院内掲示（820×820px） |
| `リハ評価ツール_QRコード.svg` | 印刷用。拡大しても輪郭が崩れない |

誤り訂正レベルは **H（最高）** なので、多少汚れても読み取れる。
生成にはこのリポジトリの既存ツールに入っているQR生成器を使った（スタッフが普段読み取っているものと同じ仕組み）。

> **URLが変わったら作り直すこと。** QRコードの中身は `https://rehab-tools-web.pages.dev/` で固定されている。
> 印刷して配る場合は、**印刷したもので一度読み取り確認をしてから**配布すること。


```
リハ評価ツールの保存先が変わりました。
お手数ですが、下記の手順で入れ直しをお願いします。

【新しいURL】
https://rehab-tools-web.pages.dev

【手順】
1. 上のURLをSafariで開く
2. メールアドレスの入力を求められたら、ご自身のアドレスを入力
3. そのアドレスに6桁の数字が届くので、入力する
4. ツールが開いたら、画面下の共有ボタン（□に↑のマーク）→「ホーム画面に追加」
5. 以降はホーム画面のアイコンから開く

数字の入力は月1回程度です。電波のない場所でも今までどおり使えます。

※ 保存中の下書き（A/B/C）は新しいURLには引き継がれません。
   古い方で入力途中のものがあれば、先に終わらせてください。

※ 6桁の数字が届かないときは、迷惑メールフォルダをご確認ください。
   それでも届かない場合は、登録したアドレスと違う可能性があるのでご連絡ください。

※ アイコンから開いたときに、もう一度メールアドレスを聞かれることがあります。
   その画面でそのまま入力すれば大丈夫です。
```

---

## 困ったとき

| 症状 | 対処 |
|---|---|
| 6桁の数字が届かない | 迷惑メールフォルダを確認。届かなければ、そのアドレスがポリシーに入っているか管理画面で確認（**許可外のアドレスには届かない仕様**） |
| ホーム画面アプリで認証がループする | 一度アイコンを削除 → Safariで開いて認証 → 改めてホーム画面に追加 |
| オフラインで開けない | 一度オンラインで全ツールを開いてキャッシュさせる |
| 特定の人だけ入れない | Access controls → Applications → ポリシーにそのアドレスがあるか確認 |
| 端末を紛失した | Zero Trust → **Team & Resources** → **Users** → 該当ユーザーのセッションを失効 |
| ツールが `ERR_FAILED` で開けない | 古い保存データが残っている。InPrivateで開くか、サイトデータを削除して再読み込み |

## 日々の管理

| やること | 場所 | 反映 |
|---|---|---|
| 新人を追加 | Access controls → **Policies** → 行の `...` → **Configure** → Emails 欄にアドレスを入力して Enter → Save | 即時 |
| 退職者を外す | 同上、アドレスの `✕` を押して削除 → Save | 即時 |
| 紛失端末を締め出す | Team & Resources → Users → セッション失効 | 即時 |

> **ポリシーの編集は `...` → Configure からしか入れない。**
> ポリシー名をクリックしても、アプリケーション側の Policies タブから開いても、
> 表示されるのは閲覧専用の画面で編集欄が出てこない。ここで必ず迷うので注意。

> アドレスの綴りを必ず確認すること。1文字違うと、その人には6桁の数字が届かない
> （許可リストにないアドレスとして扱われるため）。

## 注意点

- **Access はアクセスログを残す**（誰がいつ開いたか）。職員のアクセス記録が残ることになるため、
  院内規程上問題ないか確認しておくこと。**Free プランでの保持は24時間**なので、長期の記録は残らない。
- 無料枠は**50ユーザー**。端末数ではなく人数でカウントされる。
- 移行しても、評価尺度の著作権の論点そのものは残る（閲覧者が限定されるためリスクは大きく下がるが、
  権利者への確認や、判定基準文を載せない形への変更は別途検討する）。
- Cloudflare の管理画面は改装が続いており、**項目名や場所が変わりやすい。**
  見つからないときは **Ctrl + K**（Quick search）で項目名を検索するのが一番早い。
